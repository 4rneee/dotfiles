[module/filesystem]
type                                =   internal/fs
interval                            =   25

mount-0                             =   /

label-mounted                       =   %free%

label-unmounted                     =   %mountpoint% not mounted
label-unmounted-foreground          =   ${colors.disabled}

format-mounted-prefix               =   " "
format-mounted-prefix-foreground    =   ${colors.prefix}
format-mounted                      =   <label-mounted>
